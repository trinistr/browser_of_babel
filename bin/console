#!/usr/bin/env ruby
# frozen_string_literal: true

# Set up LOAD_PATH with Bundler
require "bundler/setup"

# Load this gem
gem_entry = Dir["#{__dir__}/../lib/*.rb"].first
require_relative gem_entry

L = BrowserOfBabel::Locator

require "irb"
IRB.start(__FILE__)
